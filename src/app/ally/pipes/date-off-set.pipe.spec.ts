import { DateOffSetPipe } from './date-off-set.pipe';

describe('DateOffSetPipe', () => {
  it('create an instance', () => {
    const pipe = new DateOffSetPipe();
    expect(pipe).toBeTruthy();
  });
});
